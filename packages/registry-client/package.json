{
  "name": "@mello/registry-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Typed fetch client for the mello registry API. Consumed by apps/web.",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@mello/plugin-spec": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.4"
  }
}
